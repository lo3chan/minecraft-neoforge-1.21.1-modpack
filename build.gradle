plugins {
    id 'java'
}

allprojects {
    group = 'com.modpack'
    version = '1.0.0'

    tasks.withType(JavaCompile).configureEach {
        options.encoding = 'UTF-8'
        options.release.set(21)
    }
}

tasks.register('buildAllMods') {
    group = 'modpack'
    description = 'Assembles and builds all integrated NeoForge 1.21.1 mod subprojects.'
    
    doLast {
        println 'Master build pipeline triggered for all integrated mods in /mods (NeoForge 1.21.1 / Java 21).'
    }
}
